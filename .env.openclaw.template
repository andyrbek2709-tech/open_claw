# OpenClaw Railway deployment — key template (no values, safe to commit)
# Copy to .env.openclaw and fill in values for manual re-deploy recovery

# --- Phase 2: Railway service env vars ---
OPENCLAW_VERSION=latest
OPENCLAW_GATEWAY_PORT=18789
OPENCLAW_GATEWAY_TOKEN=
SETUP_PASSWORD=
OPENCLAW_STATE_DIR=/data/.openclaw

# --- Phase 3: /setup wizard (stored in OPENCLAW_STATE_DIR, not Railway env) ---
ANTHROPIC_API_KEY=
TELEGRAM_BOT_TOKEN=
TELEGRAM_USER_ID=

# --- Optional: GitHub PAT for git skill ---
GITHUB_PAT=
